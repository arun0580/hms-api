# Server
NODE_ENV=development
PORT=4000

# Database (must match hms-docker/.env)
DATABASE_URL="postgresql://hms:hms_password@localhost:5433/hms?schema=public"

# JWT
JWT_SECRET="change-me-in-production-please-use-a-long-random-string"
JWT_EXPIRES_IN="7d"

# CORS
CORS_ORIGIN="http://localhost:3000"
